package com.edp.shared.kafka.model;

/**
 * Defines the different types of submissions that can trigger a notification.
 */
public enum SubmissionType {
    LEARNING,
    BLOG,
    WIKI,
    TAG,
    PACKAGE
}